# PostgreSQL Configuration
DB_HOST=postgres
DB_PORT=5432
DB_NAME=nightbot
DB_USER=nightbot_user
DB_PASSWORD=nightbot_password

# Authentication (optionnel, peut aussi être dans config.php)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme

# API Token (optionnel)
API_TOKEN=your-secret-token-here

# Application
APP_ENV=production
APP_DEBUG=false
